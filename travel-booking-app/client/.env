# Base URL of your production backend API (without the /api suffix)
# Uncomment and set this for production builds (e.g., Vercel deployment).
# For local development, leave commented out — the fallback is http://localhost:3000
# VITE_API_URL=https://booking-9kuk.vercel.app
